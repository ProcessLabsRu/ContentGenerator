# 🚀 Быстрый старт: Настройка PocketBase коллекций

## Вариант 1: Автоматическая настройка через скрипт (Рекомендуется)

### Шаг 1: Настройте учетные данные администратора

Создайте файл `.env.pocketbase` (скопируйте из `.env.pocketbase.example`):

```bash
cp .env.pocketbase.example .env.pocketbase
```

Отредактируйте `.env.pocketbase` и укажите ваши учетные данные:

```env
POCKETBASE_ADMIN_EMAIL=ваш_email@example.com
POCKETBASE_ADMIN_PASSWORD=ваш_пароль
POCKETBASE_URL=https://pocketbase.processlabs.ru/
```

### Шаг 2: Запустите скрипт

```bash
# Загрузите переменные окружения и запустите скрипт
export $(cat .env.pocketbase | xargs) && node scripts/setup-pocketbase-collections.js
```

Скрипт автоматически:
- ✅ Проверит подключение к PocketBase
- ✅ Аутентифицируется как администратор
- ✅ Создаст коллекции `generations` и `content_plan_items`
- ✅ Настроит все поля, индексы и правила доступа

---

## Вариант 2: Импорт через Admin UI

### Шаг 1: Откройте Admin UI

Перейдите по адресу: https://pocketbase.processlabs.ru/_/

### Шаг 2: Импортируйте коллекции

1. Войдите с учетными данными администратора
2. Перейдите в **Settings** → **Import collections**
3. Загрузите файл `pocketbase_collections.json`
4. Нажмите **Import**

---

## Вариант 3: Ручное создание

Следуйте подробной инструкции в файле [`POCKETBASE_COLLECTIONS_SETUP.md`](./POCKETBASE_COLLECTIONS_SETUP.md)

---

## ✅ Проверка настройки

После создания коллекций проверьте их через API:

```bash
curl https://pocketbase.processlabs.ru/api/collections
```

Вы должны увидеть в ответе коллекции:
- `generations`
- `content_plan_items`
- `users` (системная)

---

## 🔐 Создание тестового пользователя

1. Откройте Admin UI: https://pocketbase.processlabs.ru/_/
2. Перейдите в коллекцию **users**
3. Нажмите **New record**
4. Заполните:
   - **Email**: `test@example.com`
   - **Password**: `test123456`
   - **Verified**: ✅ (отметьте галочку)
5. Сохраните

---

## 🧪 Тестирование через приложение

Запустите приложение:

```bash
npm run dev
```

Откройте: http://localhost:3000/medical

Заполните форму и нажмите **Gerar Plano** для создания тестовой генерации.

---

## 📊 Структура коллекций

### `generations`
Хранит информацию о генерациях контент-планов:
- Специализация врача
- Месяц планирования
- Цели контента
- Количество публикаций по форматам
- Статус генерации

### `content_plan_items`
Хранит отдельные посты в контент-плане:
- Заголовок
- Формат (Reels, Carrossel и т.д.)
- Болевая точка
- Структура контента
- Call to Action
- Дата публикации

---

## 🔗 Связь между коллекциями

`content_plan_items.generationId` → `generations.id`

При удалении генерации автоматически удаляются все связанные посты (cascade delete).

---

## 📚 Дополнительная документация

- [Полная инструкция по настройке](./POCKETBASE_COLLECTIONS_SETUP.md)
- [Документация PocketBase](https://pocketbase.io/docs/)
- [API Rules Guide](https://pocketbase.io/docs/api-rules-and-filters/)

---

**Готово!** Теперь ваше приложение готово к работе с PocketBase. 🎉
